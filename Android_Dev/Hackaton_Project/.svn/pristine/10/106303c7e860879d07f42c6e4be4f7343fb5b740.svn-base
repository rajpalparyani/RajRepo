package com.telenav.navservice;

/**
 * Used to monitor if client is running. 
 * According to the requirement, the nav service must exit if client is not running.
 *
 */
public interface IClientStateMonitor
{
    public boolean isClientRunning();
}
